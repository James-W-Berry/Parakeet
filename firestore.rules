rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{document=**} {
      allow read, write
    }
    match /pastSongs/{document=**} {
      allow read, write
    }
  }
}